# Environment Variables Template
# Copy this file to .env and fill in your actual values

# Flask Configuration
SECRET_KEY=your-secret-key-here-generate-with-secrets-token-hex-32

# Admin Credentials (Single User Mode)
ADMIN_EMAIL=your-admin-email@example.com
ADMIN_PASSWORD=your-secure-password-here

# Database (Optional - defaults to SQLite)
# DATABASE_URL=sqlite:///invoices.db
# For PostgreSQL: postgresql://user:password@localhost/dbname

# Gemini API (Users will provide their own keys via UI)
# No need to set here - users enter their own API keys
